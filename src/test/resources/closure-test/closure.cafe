func Account() {
	var amount = 0;
	func updateAmount(withAmnt) {
		amount = amount + withAmnt;
	}
	return {
        deposit: func(b) {
                updateAmount(b);
        },
        withdraw: func(b) {
            /* if(b > amount) {
                cmd.println("Insufficient balance");
            }else {
                updateAmount(-b);
            } */
            cmd.println("hi");
        },
        checkBalance: func() {
            cmd.println(amount);
        }
	};
}

var acc = Account();
acc.checkBalance();
acc.deposit(1000);
acc.withdraw(2000);
acc.checkBalance();